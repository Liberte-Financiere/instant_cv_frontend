[2026-06-15] — Finalisation Recherche Vectorielle & UX Portail Recruteur

What changed:
- Intégration de pgvector via Prisma sur la table CandidateProfile.
- Création de l'utilitaire d'embedding avec Gemini.
- Modification de la route de recherche pour activer le filtrage hybride (SQL + Vecteur).
- Refonte UX de la carte CV (CVCard) et de la vue liste (dashboard/list) : ajout d'un statut "Portail Recruteur" explicite avec bouton de synchronisation et info-bulle explicative.
- Fix: Correction du nommage des artifacts dans le workflow GitHub Actions (db-backup.yml) pour retirer les caractères invalides (slash).

Why:
- Permettre la recherche sémantique intelligente pour les recruteurs.
- Rendre l'interface candidat beaucoup plus claire concernant la visibilité de leur profil aux yeux des recruteurs et le fonctionnement de l'IA.
- Sécuriser les sauvegardes automatisées de base de données.

Files affected:
- prisma/schema.prisma — Configuration vectorielle.
- lib/ai/embeddings.ts — Logique d'appel API Gemini.
- lib/candidate-profile.ts — Auto-vectorisation lors de la mise en ligne.
- app/api/recruiter/search/route.ts — Algorithme de recherche hybride.
- components/dashboard/CVCard.tsx — Nouveau bouton permanent de synchronisation.
- app/dashboard/list/page.tsx — Refonte de l'UI avec toggle switches, colonne recruteur et info-bulle.
- .github/workflows/db-backup.yml — Fix artifact path.

Breaking changes:
- None.

Notes for review:
- Les anciens profils nécessitent une re-sauvegarde pour avoir leur vecteur généré.
