[2026-08-01] — CI/CD enhancements: Dependabot, PR verification, and Lighthouse CI

What changed:
- Added `.github/dependabot.yml` for automated weekly dependency updates
- Added `.github/workflows/security-audit.yml` to run `npm audit` weekly
- Added `.github/workflows/ci-pull-request.yml` to test PRs before merging to main/dev
- Added `.github/workflows/stale.yml` to automatically manage and close old issues/PRs
- Added `.github/workflows/lighthouse.yml` to measure performance/SEO on PRs and main
- Modified `build-deploy.yml` to include `npx tsc --noEmit` before tests

Why:
- Ensure dependencies are kept secure without manual intervention
- Prevent broken code or type errors from being merged and deployed to production
- Keep the repository clean by closing stale/abandoned tasks automatically
- Monitor performance regressions on the Next.js app using Lighthouse

Files affected:
- .github/dependabot.yml [NEW] — dependabot config
- .github/workflows/security-audit.yml [NEW] — weekly npm audit workflow
- .github/workflows/ci-pull-request.yml [NEW] — PR verification workflow
- .github/workflows/stale.yml [NEW] — stale issue bot workflow
- .github/workflows/lighthouse.yml [NEW] — performance measurement workflow
- frontend/instant_cv/.github/workflows/build-deploy.yml — added typescript check

Breaking changes:
- None

Notes for review:
- Workflows rely on the `npm ci` and Next.js standard caching behaviors.
- Lighthouse uses a temporary public storage (provided by Google via lhci) to store and share reports for PRs.
