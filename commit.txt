[2026-07-11] — Ajout d'un script d'automatisation pour le déploiement Docker

What changed:
- Création du script `deploy_python_bg.sh` à la racine du projet frontend.

Why:
- Pour simplifier au maximum le déploiement sur le serveur.
- Le script vérifie si Docker est installé (l'installe si besoin), build l'image de façon sécurisée, nettoie les anciens conteneurs, et lance le nouveau avec le bon binding de port (127.0.0.1:3001).
- Évite les erreurs humaines de copier-coller des commandes du Markdown.

Files affected:
- deploy_python_bg.sh — Nouveau fichier bash (exécutable).

Breaking changes:
- None.

Notes for review:
- Le script doit être lancé à la racine du projet `frontend/instant_cv` avec `bash deploy_python_bg.sh` (ou `./deploy_python_bg.sh`).
