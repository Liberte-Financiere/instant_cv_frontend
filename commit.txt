[2026-03-22] — feat: Interview Simulator Audio mode (Phase 2)

What changed:
- Built full bidirectional audio support with Gemini Live API using Server-Sent Events (SSE) and HTTP POST chunks to bypass Next.js WebSocket limitations.
- Created `lib/interview-audio.ts` to manage server-side WebSocket connections securely without leaking API keys to the frontend.
- Added a standalone `AudioControls` React component to manage microphone streaming, AudioContext playback, and UI state (listening, thinking, speaking).
- Added a UI toggle in the Chat interface to seamlessly switch between Text mode and Live Audio mode at any time.
- Updated Prisma schema to store `transcript` and `audioUrl`.
- Adjusted credit billing: `AI_INTERVIEW_AUDIO` costs 5 credits per session.

Why:
- Allows candidates to practice oral interviews interactively in a natural, conversational way via Gemini Live API's low-latency streaming.
- Next.js serverless architecture lacks robust WebSocket proxying, so splitting the streams (SSE downstream, POST chunks upstream) resolves deployment issues reliably.

Files affected:
- lib/interview-audio.ts
- components/interview/AudioControls.tsx
- app/api/ai/interview/ws/[sessionId]/start/route.ts
- app/api/ai/interview/ws/[sessionId]/chunk/route.ts
- app/interview/[sessionId]/page.tsx
- prisma/schema.prisma
- lib/credits.ts

Breaking changes:
- Required `@types/ws` and `ws` package. 

Notes for review:
- DB pushed.
- Text mode remains fully functional and intact as requested.
