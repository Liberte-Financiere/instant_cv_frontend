[2026-03-22] — fix: AI analyze/match recevait des CV tronqués en mode "select"

What changed:
- Corrigé le bug critique où l'AI recevait un CV sans experiences, education, skills, etc.
- Les pages analyze et match fetchen maintenant le CV complet via CVService.getById()
- Avant: cvList contenait uniquement des summaries (sans sections importantes)

Why:
- fetchUserCVs() appelle /api/cv?summary=true qui strip les données lourdes
- Les pages envoyaient directement ces summaries à l'IA, qui analysait un CV quasi vide

Files affected:
- app/dashboard/ai/analyze/page.tsx — fetch full CV avant envoi à l'IA
- app/dashboard/ai/match/page.tsx — fetch full CV avant envoi à l'IA

Breaking changes:
- None

Notes for review:
- Le dashboard page.tsx n'est pas affecté (utilise upload PDF uniquement)
- L'appel CVService.getById() ajoute une requête réseau avant l'analyse, mais la latence est négligeable vs le temps de réponse IA
