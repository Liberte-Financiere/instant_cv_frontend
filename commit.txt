[2026-03-24] — Interview Hub, Mobile Sync & Dashboard Persistence

What changed:
- Created a dedicated History Hub for AI Interviews with advanced filtering and search.
- Synced the mobile sidebar with the desktop navigation to include all AI tools and preferences.
- Fixed inconsistent icons in the mobile header (Brain, Mic, Zap).
- Enabled persistence for AI analyses performed from the main dashboard "Magic Analyzer".
- Limited the interview history view on the setup page to 3 items for a cleaner UI.

Why:
- To provide users with a feature-rich interface to manage their interview history.
- To ensure a consistent and full feature set across mobile and desktop devices.
- To avoid data loss and provide a complete history of AI analyses regardless of where they are initiated.

Files affected:
- app/dashboard/ai/interview/history/page.tsx — New dedicated history hub.
- app/dashboard/ai/interview/page.tsx — Limited recent history and added link to the new hub.
- components/dashboard/MobileHeader.tsx — Synced navigation links and fixed icons for mobile.
- app/dashboard/page.tsx — Added persistence call for "Magic Analyzer" results.

Breaking changes:
- None

Notes for review:
- Verify the mobile menu on a real device or responsive simulator to ensure icons are properly aligned.
- Check that analyses from the dashboard now appear in /dashboard/ai/analyze history.
