[2026-04-01] — Refactor P8: extract audio interview prompt into shared builder

What changed:
- Created `buildAudioSystemInstruction()` in lib/interview.ts
- Replaced hardcoded system instruction in ws/[sessionId]/start/route.ts with builder call
- Added jobContext support to the audio interview prompt
- Enriched prompt rules: clearer trigger behavior, explicit encouragement directives, conversational tone instruction

Why:
- The audio prompt was the only one hardcoded directly in a route instead of the shared lib
- Missing jobContext meant the AI had no visibility on the job offer details during audio interviews
- Original prompt was too sparse compared to text interview prompts (P7)

Files affected:
- lib/interview.ts — added buildAudioSystemInstruction() export
- app/api/ai/interview/ws/[sessionId]/start/route.ts — replaced inline prompt with builder call, added import

Breaking changes:
- None

Notes for review:
- The trigger message "Bonjour, commence." (line ~110 in interview-audio.ts) is still sent 500ms after setup. The new prompt rule 1 says "present yourself once audio is established" which aligns better than the old "Commence TOUJOURS" that could conflict.
- No max questions limit was added per user instruction — audio mode is open-ended by design.
