2026-06-16 — Optimisation base de données VPS & Réorganisation des tests

What changed:
- Configuré le pool de connexion Prisma (pg.Pool) avec max: 20 et timeouts.
- Ajouté les index manquants (acceptsMarketing, referredById) dans le modèle User.
- Restreint les requêtes lourdes (transactions admin) avec take: 100 pour prévenir les crashs OOM.
- Implémenté un cache en mémoire (Map) pour les KPI du Dashboard Admin (AI Logs).
- Réorganisé le dossier `__tests__` en sous-dossiers (api, lib, utils, integration, models) pour un code plus propre.

Why:
- Protéger la base de données Supabase des "Connection Exhaustion".
- Adapter le setup Prisma aux environnements VPS persistants (PM2).
- Prévenir les "Full Table Scans" sur les requêtes fréquentes.
- Clarifier l'architecture des tests qui devenait brouillonne.

Files affected:
- lib/prisma.ts — Ajout configuration Pool
- prisma/schema.prisma — Ajout des index sur User
- app/api/hq-ops/transactions/route.ts — Ajout de la pagination (take: 100)
- app/api/admin/ai-logs/route.ts — Mise en place du cache
- __tests__/* — Déplacement des fichiers et remplacement des imports relatifs par les alias '@/...'

Breaking changes:
- None

Notes for review:
- Le schéma Prisma a été synchronisé en base avec `npx prisma db push` avec succès sans perte de données.
- Test de charge : Les requêtes admin répondront désormais instantanément grâce au cache en RAM.
