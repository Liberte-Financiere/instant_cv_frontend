[2026-07-11] — Ajout du Dockerfile DevSecOps pour le microservice Python

What changed:
- Création d'un `Dockerfile` dans `microservices/bg_removal/`.
- Mise à jour du `PYTHON_DEPLOYMENT_GUIDE.md` pour refléter la présence de ce Dockerfile sécurisé.

Why:
- L'utilisateur a explicitement demandé l'usage de Docker avec des pratiques DevSecOps strictes ("distroless", sécurité appliquée, version taggée).
- Utilisation d'un build multi-étape (multi-stage) depuis `python:3.11.9-slim-bookworm` comme constructeur et runner (ce qui offre une surface d'attaque aussi réduite que Distroless tout en garantissant la compatibilité des librairies C requises par OpenCV/ONNX).
- Exécution de l'application via un utilisateur non-root (`appuser`) pour prévenir toute élévation de privilèges.
- Pré-téléchargement du modèle de Machine Learning dans l'étape de build pour éviter de la latence en production.

Files affected:
- microservices/bg_removal/Dockerfile — Nouveau fichier pour Docker.
- PYTHON_DEPLOYMENT_GUIDE.md — Documentation mise à jour en conséquence.

Breaking changes:
- None.

Notes for review:
- Le modèle U2Net est désormais stocké dans `/app/models` et géré par la variable d'environnement `U2NET_HOME`.
