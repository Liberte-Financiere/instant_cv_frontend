2026-06-06 — Harmonize Comparison Color Badges and Search Elements

What changed:
- Updated `CompareVerdictBadge` colors to use a premium emerald theme (`bg-emerald-50 text-emerald-700 border-emerald-200/60`) for positive outcomes.
- Configured neutral competitor badges to use semi-transparent slate backgrounds (`bg-slate-100/60 text-slate-700 border-slate-200/50`).
- Changed the "equality" draw badge to use the core brand primary color with transparency (`bg-primary/5 text-primary border-primary/20`).
- Updated category button states on `CompareSearch` to implement a glowing brand primary shadow (`shadow-primary/20`) when active.
- Verified TypeScript compilation successfully with 0 errors.

Why:
- To match brand identity guidelines and improve color harmony across competitor comparison tables and search filters.

Files affected:
- frontend/instant_cv/components/compare/CompareVerdictBadge.tsx — Verdict badge color configurations
- frontend/instant_cv/components/compare/CompareSearch.tsx — Search category button shadow glows
