[2026-08-23] — Analytics Tracking & Hybrid System Robustness

What changed:
- Added click tracking capabilities for native (Prisma) jobs.
- Refactored job ID prefix from "go-" to "ext-" to semantically differentiate scraped jobs from internal ones.
- Added comprehensive unit test coverage (Vitest) for the Next.js API layer handling hybrid job data.
- Added Go test coverage for the tracking handler in the jobs_api microservice.
- Verified Next.js production build and overall test suite (301 tests passing).

Why:
- To ensure full tracking of both internal and external job applications, which is essential for the recruiter analytics dashboard.
- To prevent regressions when routing or processing hybrid IDs by enforcing strict test coverage.
- To maintain clean architecture by replacing generic "go-" prefixes with semantic "ext-" identifiers.

Files affected:
- app/jobs/[id]/page.tsx — Added onClick tracking for native apply buttons.
- app/api/jobs/route.ts — Renamed "go-" prefix to "ext-".
- app/api/jobs/[id]/route.ts — Renamed prefix, added tracking logic for Prisma.
- __tests__/api/jobs/jobs-route.test.ts — New unit test for hybrid GET API.
- __tests__/api/jobs/job-detail-route.test.ts — New unit test for native tracking API.
- microservices/jobs_api/internal/handlers/tracking_test.go — New unit test for Go tracking.
- microservices/jobs_api/go.mod — Updated via go mod tidy to add testify.

Breaking changes:
- None. Any external systems relying on "go-" prefix (none expected) will now receive "ext-".

Notes for review:
- The React Testing Library was avoided for UI tests to prevent unnecessary dependency pollution, focusing purely on API robustness as requested.
