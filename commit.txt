feat: implémentation des titres de sections personnalisables et pagination du dashboard

- Ajout de la propriété `sectionTitles` dans le store Zustand et les types CV.
- Mise à jour de `getSectionTitle` pour supporter la personnalisation et le fallback.
- Déploiement de la logique dans les 20+ templates et le SectionRenderer.
- Ajout d'un champ "Titre de la section (Optionnel)" dans tous les formulaires de l'éditeur.
- Amélioration du bouton Debug avec plus de données mockées (pour tests de charge).
- Ajout de la pagination (10 éléments/page) et du tri (plus récent d'abord) sur la liste des CVs du dashboard.
