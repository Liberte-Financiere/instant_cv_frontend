[2026-08-03] — FIX: AI History Navigation Routes

What changed:
- Updated the routing paths in `handleItemClick` inside `HistoryList.tsx`.

Why:
- The component was incorrectly pushing users to the tool index pages (`/dashboard/ai/analyze` and `/dashboard/ai/match`) instead of the actual results pages (`/analysis` and `/match`). Because React router is an SPA router, pushing to the current active path causes no visible action, making the UI appear unresponsive.

Files affected:
- frontend/instant_cv/components/dashboard/ai/HistoryList.tsx — corrected router.push targets for 'analysis' and 'match' history items.

Breaking changes:
- None

Notes for review:
- The 'bilan' route was already correctly pointing to `/dashboard/tools/bilan` and was not changed.
