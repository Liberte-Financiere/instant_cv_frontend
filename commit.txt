fix: refactor en profondeur de la logique des suggestions IA (Match)

- Correction du chargement asynchrone dans `handleApplyReformulation` (évite de modifier un CV vide).
- Support étendu des sections (education, skills, projects, certifications) au lieu de juste experience/personalInfo.
- Sécurisation contre les crashs liés aux index hors limites (utilise l'état en temps réel du store).
- Mise à jour du prompt système (API) pour inclure les projets et certifications dans le schéma JSON strict de Gemini.
